# AI 작업 안내 (이 폴더를 받은 AI가 먼저 읽는 문서)

이 폴더는 강사가 **강의 하나를 만들고, 그 강의를 알리는 콘텐츠(블로그·스레드·카드뉴스·숏폼 기획)**까지 AI와 함께 만드는 키트다. 사용자는 대부분 개발자가 아니다. 쉬운 말로, 한 번에 한 가지씩 진행한다.

## 사용자가 "안내문 읽고 설치해줘"라고 하면

설치할 프로그램은 거의 없다. 아래 순서로 진행한다.

1. 이 폴더의 `README.md`를 읽고, 무엇을 할 수 있는지 사용자에게 세 줄 이내로 알려준다.
2. 사용자에게 묻는다: **"오늘 무엇을 만들까요?"** 선택지를 보여준다.
   - 강의 만들기 (자료조사 → 내 데이터 → 구조 설계서 → 장표 → 프롬프터 → 배포자료)
   - 이미 있는 강의를 알리기 (블로그·스레드 / 카드뉴스 / 숏폼 기획 / 녹음을 글로)
3. 사용자가 장표부터 원하면, 1·2단계(자료조사·내 데이터)를 건너뛴다고 한 줄로 알려주고 구조 설계서에 필요한 세 가지(강의 제목 / 강의 시간 / 끝나면 할 수 있는 것 한 줄)를 묻는다.
4. 고른 일에 해당하는 폴더의 `안내.md`를 읽고 그 순서대로 진행한다.
   - 강의 만들기: `01-강의만들기/README.md` → 단계별 폴더
   - 알리기: `02-알리기/README.md` → 단계별 폴더
5. 장표 단계에서는 `.claude/skills/lecture-deck/SKILL.md`(같은 내용이 `01-강의만들기/4-장표/lecture-deck/`에도 있음)의 규칙을 따른다.
6. 카드뉴스를 이미지로 뽑을 때만 Chrome·Chromium·Edge 중 하나가 필요하다. 없으면 설치를 강요하지 말고, HTML을 브라우저로 열어 화면 캡처하는 방법을 안내한다.
7. 압축 해제나 파일 목록 확인에 터미널 `unzip`을 쓰지 않는다(한글 파일 이름에서 실패한다). 사용자에게는 맥 더블클릭, 윈도우 마우스 오른쪽 버튼 → 압축 풀기를 안내한다.
8. 윈도우에서 카드뉴스를 이미지로 뽑을 때는 `render.sh` 실행을 먼저 권하지 말고, HTML을 Chrome으로 열어 화면을 캡처하는 방법을 안내한다. 실행했다면 만들어진 PNG를 직접 열어 오류 화면이 아닌지 확인한다.

## 반드시 지킬 것

- 만드는 모든 화면·장표·카드뉴스는 글꼴을 Pretendard로 한다. HTML을 만들 때 `<link rel="stylesheet" href="https://cdn.jsdelivr.net/gh/orioncactus/pretendard@v1.3.9/dist/web/static/pretendard.min.css">`를 넣어 실제로 불러오게 하고, font-family 첫 자리에 Pretendard를 둔다.
- **사용자 자료로만 쓴다.** 사용자가 준 강의자료, 녹음 받아쓴 글, 자주 받는 질문, 대화 내용이 재료다.
- **없는 숫자·후기·성과·수강생 반응을 만들지 않는다.** 필요하면 빈칸 `[ ]`으로 두고 사용자에게 묻는다. 추측이면 추측이라고 표시한다.
- **한 번에 끝내지 않는다.** 단계마다 결과를 보여주고 사용자 확인을 받은 뒤 다음 단계로 넘어간다. 구조 설계서 확인 전에는 장표를 만들지 않는다.
- **예시 파일은 형식 참고용이다.** `예시-*.md`의 문장·숫자를 사용자 결과물에 옮기지 않는다.
- 사용자 결과물은 이 키트 폴더 밖이나 `내작업/` 폴더에 저장하고, 키트 원본 파일은 덮어쓰지 않는다. 저장한 파일 경로를 알려준다.
- 수강생 실명, 연락처, 결제 정보 같은 개인정보는 결과물에 넣지 않는다. 사용자가 붙여 넣은 자료에 있으면 가리고 쓰겠다고 먼저 말한다.
- 다른 사람의 글·이미지·목소리를 그대로 복제하지 않는다. 레퍼런스는 분석해서 규칙으로만 쓴다.
- 게시, 발송, 결제, 유료 API 호출은 하지 않는다. 필요하면 사용자에게 무엇을 왜 하는지 설명하고 직접 하도록 안내한다.
- 이모지를 쓰지 않는다. 사용자 말투를 따른다. 따로 말이 없으면 짧은 존댓말.

## 사용자가 막혔을 때

- 결과가 마음에 안 든다고 하면: 어디가 어떻게 마음에 안 드는지 한 가지만 묻고 그 부분만 고친다.
- 각 단계 `안내.md`의 "마음에 안 들면 이렇게 말하세요" 문장을 예시로 보여준다.
- 파일이 어디 있는지 모르겠다고 하면: 전체 경로를 다시 알려준다.
